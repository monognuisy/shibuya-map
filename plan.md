# shibuya-map — 시부야역 입체 보행 네트워크

시부야역 주변의 건물·개찰·통로를 **실제 좌표(미터)** 위에서 층별로 보여주고,
두 지점 사이의 보행 경로를 층 이동까지 포함해 안내하는 웹 서비스.

기존 단일 HTML 프로토타입(`shibuya-station-3d.html`, 노드 46 / 엣지 70, 좌표는 손으로 찍은 추상 단위)을
공개 오픈데이터 기반의 실측 네트워크로 교체하고, Astro + Svelte 프로젝트로 재구성한다.

---

## 1. 오픈데이터 조사 결과 (2026-08-30 확인)

### 1-1. 결론

> **시부야역 "실내(구내) 지도" 오픈데이터는 공개되어 있지 않다.**
> 대신 **시부야 지구 옥외 보행공간 네트워크**는 국토교통성이 공식 공개하고 있으며, 이것을 기반 네트워크로 채택한다.

국토교통성 「屋内地図オープンデータ」(계층별 실내 지리공간정보 데이터 사양 준거, Shapefile/GeoPDF)로
실제 공개된 지구는 G공간정보센터 CKAN 기준 아래 11건이 전부이며 **시부야역은 포함되지 않는다.**

| 공개 지구 | 데이터셋 ID |
|---|---|
| 도쿄역 주변 (令和元/2년도) | `mlit-indoor-tokyo`, `mlit-indoor-tokyo-r2` |
| 신주쿠역 주변 (H29/H30/令和2년도) | `mlit-indoor-shinjuku*` |
| 신요코하마역 / 요코하마국제종합경기장 | `mlit-indoor-shin-yokohama`, `mlit-indoor-yokohama-arena` |
| 나리타공항 (令和元/2년도) | `mlit-indoor-narita-airport*` |
| 나고야 유니몰·센트럴파크 지하가 | `city-nagoya-indoor-*` |

ODPT(공공교통 오픈데이터센터)의 역 구내지도 GeoJSON도 **도영 오에도선 계열 역 위주**로,
시부야역(JR / 도쿄메트로 / 도큐 / 게이오)은 대상 밖이다.
각 사업자 공식 구내도는 PDF/이미지이며 오픈 라이선스가 아니다.

### 1-2. 채택 데이터 소스

| # | 데이터 | 출처 | 형식 | 라이선스 | 용도 |
|---|---|---|---|---|---|
| A | **歩行空間ネットワークデータ（渋谷地区）** 노드 860 / 링크 982 | 국토교통성 「歩行者移動支援サービスに関するデータサイト」 dataset 65 / resource 223·224 | SHP + CSV, JGD2011 경위도 | 정부표준이용규약 2.0 (CC BY 4.0 호환) | **기반 옥외 보행 네트워크**. 실거리(distance), 폭원(width), 종단구배, 단차, 계단/경사, EV 유무, 지붕 유무 속성 보유 |
| B | 施設データ（渋谷地区） | 위와 동일 dataset 65 / resource 225·226 | SHP + CSV | 동일 | 시설 POI, 배리어프리 설비 |
| C | **OpenStreetMap** 건물 폴리곤 · 승강장 · 실내 통로 | Overpass API | GeoJSON | ODbL 1.0 | 건물 외곽선(스크램블스퀘어·히카리에·마크시티·스트림·사쿠라스테이지 등), 승강장 형상, `level` 태그 |
| D | (선택) PLATEAU 3D도시모델 渋谷区 2025 | G공간정보센터 | CityGML 650MB | PLATEAU 사이트 정책 | 건물 실측 높이 보강. **1차 범위 제외** (용량 대비 이득 낮음, OSM `height`/`building:levels`로 대체) |

**A 데이터의 한계 (반드시 명시할 것)**

- `in_out` 전 노드 = 1 → **전부 옥외**. 역 구내·지하 개찰 안쪽은 포함되지 않음.
- `floor` 값은 0 / 0.5 / 1 세 종류뿐 (지상·중2층·데크). 지하 레벨 없음.
- 링크는 전부 2점 폴리라인(= 노드-노드 직선). 곡선 통로 형상은 없음.
- 즉 A만으로는 "시부야역 입체 구조"를 표현할 수 없다.

### 1-3. 따라서: 2계층 데이터 모델

```
official  ── A(보행공간 네트워크) + B(시설) + C(OSM 건물/승강장)  … 출처 검증 가능
curated   ── 역 구내 레이어(승강장·개찰·연락통로·수직동선)         … 직접 작성, 실좌표에 앵커
```

`curated` 레이어는 기존 프로토타입의 지식을 계승하되,
**추상 x/z 좌표가 아니라 실제 위경도로 다시 찍고**, 통로는 **폴리라인**으로 모델링한다.
모든 피처는 `provenance: 'official' | 'curated'` 를 갖고 UI에서 구분 표시한다.
(공식 데이터인 척 하지 않는 것이 이 프로젝트의 원칙)

---

## 2. 좌표계

- 저장·배포 원본: **WGS84 / JGD2011 경위도** (두 좌표계 차이는 이 축척에서 무시 가능)
- 렌더 및 연산: **로컬 ENU 평면 미터**
  - 원점 `ORIGIN = { lat: 35.658034, lon: 139.701636 }` (JR 시부야역 중앙 부근)
  - 자오선/묘유선 곡률반경 기반 변환 → 반경 2km 내 상대오차 < 1e-6
  - `x` = 동쪽(m), `y` = 북쪽(m), `z` = 층 높이(m)
- 층 높이: 지상 1층 = 0m 기준, 층당 기본 5.0m (지하는 층당 6.0m).
  실제 시부야역 층고 편차는 `levels.ts` 에 층별 실측 근사값으로 테이블화.

---

## 3. 아키텍처

```
shibuya-map/
├─ plan.md
├─ data/
│  ├─ raw/                     # 원본 그대로 (재현성)
│  │  ├─ mlit-pnw/             # A: node.csv, link.csv, *.shp
│  │  ├─ mlit-facility/        # B
│  │  └─ osm/                  # C: overpass 응답
│  └─ build/                   # 생성물 (git 포함, 빌드 재현 가능)
│     ├─ network.json          # 통합 그래프 (미터 좌표)
│     ├─ buildings.json        # 건물 폴리곤 (미터 좌표)
│     └─ meta.json             # 소스별 취득일시·라이선스·해시
├─ scripts/
│  ├─ fetch.ts                 # A/B/C 다운로드 → data/raw
│  └─ build.ts                 # raw → build (투영·병합·검증)
├─ src/
│  ├─ lib/
│  │  ├─ geo.ts                # ENU 투영, 거리
│  │  ├─ levels.ts             # 층 정의·높이
│  │  ├─ graph.ts              # 그래프 구축 + Dijkstra(가중치=시간)
│  │  └─ types.ts
│  ├─ data/curated/            # 역 구내 레이어 (수기, 실좌표)
│  │  ├─ nodes.ts              # 승강장/개찰/광장/수직동선
│  │  ├─ passages.ts           # 연락통로 폴리라인
│  │  └─ structures.ts         # 건물 메타(개업연도·설명·운영자)
│  ├─ components/              # Svelte
│  │  ├─ Scene.svelte          # three.js 렌더러
│  │  ├─ LevelRail.svelte      # 층 선택
│  │  ├─ Filters.svelte        # 사업자/종류 필터
│  │  ├─ RoutePanel.svelte     # 출발/도착·경로 결과
│  │  ├─ Inspector.svelte      # 선택 피처 상세
│  │  └─ Legend.svelte
│  └─ pages/index.astro
└─ public/
```

- **Astro 5** (정적 출력) + **Svelte 5 (runes)** 아일랜드. 지도 UI 전체가 하나의 `client:only` 아일랜드.
- **three.js** 로 층별 평면을 쌓아 올린 2.5D/3D 뷰 (기존 프로토타입의 강점 유지).
- 데이터는 빌드타임에 JSON 으로 고정 → 런타임 외부 요청 0.

---

## 4. 렌더링 / UI 방침

기존 UI의 단순함·다크 테마·모노스페이스 감성을 유지하되, 구조물 구분을 강화한다.

- **건물**: OSM 폴리곤을 층 높이만큼 압출(extrude). 반투명 면 + 진한 외곽선.
- **승강장**: 실제 방향/길이를 가진 가늘고 긴 박스. 노선색.
- **개찰**: 작은 정육면체 + 사업자색 테두리.
- **통로**: **폴리라인 튜브**. 층이 바뀌는 구간은 수직 세그먼트로 꺾어 표현(현행 L자 대신 실제 경사/계단 위치 반영).
- **보행 네트워크(A)**: 얇은 회색 라인. 토글로 on/off. 켜면 실측 도로망이 배경으로 깔림.
- **구분**: 사업자색 + 종류별 형상 + `official/curated` 배지.
- **층**: 레일에서 층 선택 시 해당 층 강조, 나머지는 감광. "전체" 모드에서 입체 확인.
- 상시 표시 정보는 줄이고, 클릭 시 Inspector 에서 상세.

---

## 5. 경로 탐색

- 그래프 = A의 링크 + curated 통로/수직동선.
- 가중치 = **보행 시간(초)**: 수평 `거리 / 1.25 m/s`, 계단 상행 `+0.6 s/단`, 에스컬레이터/EV 는 대기+이동 상수.
- 옵션: 배리어프리(계단 회피 = A의 `lev_diff`/`vtcl_slope` 및 curated `stairs` 플래그 사용).
- 결과: 총 거리·시간·층 프로파일·경유 안내 텍스트 + 3D 하이라이트.

---

## 6. 작업 순서

- [x] 0. 오픈데이터 존재 여부 조사 → 실내지도 부재 확인, 보행공간 네트워크 확보
- [x] 1. `git init` + 프로젝트 스캐폴딩 (Astro 5 + Svelte 5 + TS)
- [x] 2. `scripts/fetch.ts` — A/B/C 취득, `sources.json` 에 출처·취득일시·sha256 기록
- [x] 3. `src/lib/geo.ts` — ENU 투영 + 왕복/측지거리 대조 테스트
- [x] 4. `scripts/build.ts` — raw → `map.json`, 레이어 접합과 연결성 검증
      (주 네트워크에 닿지 않는 curated 지점이 있으면 빌드 실패)
- [x] 5. curated 역 구내 레이어를 실좌표로 작성.
      승강장 선형은 OSM 궤도·승강장 폴리곤에 앵커링, 통로는 폴리라인 → 폴리곤
- [x] 6. three.js 씬 — 층 스택, 실제 폴리곤 슬래브, 주요 건물 압출, 수선
- [x] 7. UI — 층 레일, 레이어/사업자 필터, Inspector, 출처 시트
- [x] 8. 경로 탐색 + 결과 패널 (배리어프리 · 공사중 포함 옵션)
- [x] 9. 출처 표기 화면, README, GitHub Pages 배포 워크플로

### 이후 할 만한 것

- [x] 건물을 접속 층마다 노드로 쪼개고 내부 수직 동선으로 연결 (다중 시작 다익스트라)
- [x] 개찰 안/밖을 구분해 「개찰 안쪽을 지름길로 쓰지 않기」 옵션 추가
- [x] 승강장·연락통로를 노드 체인으로 확장하고, 링크를 가장 가까운 지점에 접속
- [x] OSM 수직 동선(계단·에스컬레이터·EV)을 개별 시설로 분리
- [x] 경유지 단위 상세 안내 + 경로 따라가기(진행 방향 시점)
- [ ] curated 개찰·광장을 대표점 하나가 아니라 폴리곤/다중 출입점으로 확장
      (지금은 그 지점을 지나는 경로 거리가 실제보다 짧게 나올 수 있음)
- [ ] OSM 실내 보행로에도 개찰 안/밖 판정 적용
- [ ] 층 표고를 각 사업자 단면 자료로 보정
- [ ] PLATEAU 3D도시모델(渋谷区)로 건물 높이·형상 보강 (CityGML 650MB → 시부야역
      주변만 잘라내는 전처리 필요)
- [ ] 혼잡 시간대 가중치, 도착 시각 기반 안내

---

## 7. 라이선스 표기 (필수)

- 보행공간 네트워크·시설 데이터: 국토교통省 「歩行者移動支援サービスに関するデータサイト」/ 정부표준이용규약(제2.0판)
- 건물·승강장 형상: © OpenStreetMap contributors / ODbL 1.0
- curated 레이어: 본 프로젝트 자체 작성물. 공식 데이터가 아님을 UI에 명시.
